# 💝 Sois ma Valentine ?

Petite page interactive « Veux-tu être ma Valentine ? » avec React et Tailwind CSS.

- **OUI** : message de succès avec bisous
- **Non** : le bouton s’échappe quand on essaie de cliquer 😄

## Déploiement sur GitHub Pages

1. Crée un dépôt sur GitHub (ex. `valentine`).
2. Dans le dépôt : **Settings** → **Pages**.
3. **Source** : « Deploy from a branch ».
4. **Branch** : `main` (ou `master`), dossier **/ (root)**.
5. Sauvegarde. Le site sera à :  
   `https://<ton-username>.github.io/valentine/`

## Lancer en local

Ouvre simplement `index.html` dans un navigateur, ou utilise un serveur local :

```bash
# Avec Python
python -m http.server 8000

# Avec Node (npx)
npx serve .
```

Puis va sur http://localhost:8000
